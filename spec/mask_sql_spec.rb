require "spec_helper"

RSpec.describe MaskSql do
  it "has a version number" do
    expect(MaskSql::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
